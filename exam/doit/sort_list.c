
typedef	struct s_list
{
	
}t_list;
/*
4
Assignment name  : sort_list
Expected files   : sort_list.c
Allowed functions: 
--------------------------------------------------------------------------------

다음 함수를 작성하십시오.

t_list * sort_list (t_list * lst, int (* cmp) (int, int));

이 함수는 함수를 사용하여 매개 변수로 제공된 목록을 정렬해야합니다.
적용 할 순서를 선택하려면 포인터 cmp를
정렬 된 목록의 첫 번째 요소.

중복이 남아 있어야합니다.

입력은 항상 일관성이 있습니다.

파일 목록에 설명 된 t_list 유형을 사용해야합니다.
그것은 당신에게 제공됩니다. 해당 파일을 포함해야합니다
(#include "list.h"), 설정해서는 안됩니다. 우리는 우리 자신을 사용할 것
과제를 컴파일합니다.

cmp로 전달 된 함수는 항상 다른 값을 반환합니다.
a와 b가 올바른 순서이면 0, 그렇지 않으면 0

예를 들어 cmp로 사용 된 다음 함수는 목록을 정렬합니다.
오름차순으로 :

int ascending(int a, int b)
{
    return (a <= b);
}


*/