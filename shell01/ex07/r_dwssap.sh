cat /etc/passwd | grep -v "#" | cut -f 1 -d ":" | sed -n "2, \$p" | rev | sort -r | awk -v start=${FT_LINE1} -v end=${FT_LINE2} "NR>=start && NR<=end" | tr '\n' ',' | sed 's/,/, /g' | sed 's/, *$/\./' | tr -d '\n'
